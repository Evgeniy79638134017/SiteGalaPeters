# PROMPT_081_cookie_policy

> Конвенции (чтение CLAUDE.md, QA-гейты, отчёт/CHANGELOG/SPRINT/коммит-пуш) — как в CLAUDE.md,
> не повторяю. Ниже только специфика задачи.

## Контекст
TASK-081. Нужна страница «Политика в отношении файлов cookie» под требование 152-ФЗ об
информировании. Обоснование/что должно быть в тексте: siteGala/COOKIE_CONSENT_RESEARCH.md (§1, §3).
Факт на сейчас: аналитика — только Umami (cookieless, без куки); Яндекс.Метрика/GA ОТЛОЖЕНЫ
(решение владельца 11.06.2026), баннера ещё нет (TASK-080 — позже). Значит текст описывает
фактическое состояние, без зависимости от баннера.

## Критерии приёмки
1. Роут `/cookie-policy` — серверная страница ТОЧНО по шаблону `src/app/privacy/page.tsx`
   (тот же контейнер и классы: `<main className="min-h-screen bg-warm-bg py-16 px-6">`,
   `<article className="max-w-3xl mx-auto space-y-8">`, `h1/h2 text-teal`, `p text-text-muted`),
   + `export const metadata` (title/description), индексируется (без noindex).
2. Разделы (RU, понятным языком): что такое cookie; какие cookie сайт использует СЕЙЧАС —
   только необходимые/технические (сессия, безопасность, запоминание выбора cookie);
   аналитика Umami — без куки и без идентификации; аналитические/маркетинговые cookie
   (Метрика, GA, рекламные пиксели) — «могут использоваться в БУДУЩЕМ только с вашего согласия,
   сейчас не активны»; как отключить cookie в браузере; право отозвать согласие; контакты
   оператора (EMAIL_CONTACT из src/lib/constants.ts) + ссылка на /privacy.
3. Ссылка на /cookie-policy добавлена в Footer рядом с «Политикой конфиденциальности».
4. В /privacy добавлена короткая встречная ссылка на /cookie-policy (чтобы документы не
   противоречили).
5. `/cookie-policy` добавлен в `src/app/sitemap.ts` (по аналогии с /privacy: priority 0.3,
   changeFrequency "yearly").

## Риски/специфика (из ревью кода 11.06.2026)
- Образец вёрстки и паттерн метаданных уже есть в `src/app/privacy/page.tsx` — переиспользовать,
  не выдумывать дизайн.
- НЕ добавлять кнопку «Настройки cookie» / вызов CookieConsent — баннера пока нет. Вместо этого
  раздел «как управлять cookie» описывает отключение через браузер. Кнопку добавим в TASK-080.
- EMAIL_CONTACT и ссылки — из constants, не хардкодить.

## DON'T
- Не описывать Метрику/GA как уже работающие. Не ставить noindex. Не дублировать текст
  /privacy — ссылаться. Не делать страницу клиентской (она статическая).
