# REPORT TASK-081: Страница /cookie-policy (информирование по 152-ФЗ) — 2026-06-11

**Агент:** VS Code Claude
**Статус:** Done

## Что сделано

Добавлена статическая серверная страница «Политика в отношении файлов cookie», описывающая
ФАКТИЧЕСКОЕ состояние (Umami cookieless; Метрика/GA отложены и не активны; баннера ещё нет).
Текст и состав разделов — по COOKIE_CONSENT_RESEARCH.md §1 (152-ФЗ: cookieless-аналитика
баннера не требует, но информирование/Политика cookie обязательны) и §3.

- **`src/app/cookie-policy/page.tsx`** — серверный компонент ПО ШАБЛОНУ `privacy/page.tsx`
  (тот же контейнер/классы: `<main className="min-h-screen bg-warm-bg py-16 px-6">`,
  `<article className="max-w-3xl mx-auto space-y-8">`, `h1/h2 text-teal`, `p text-text-muted`),
  `export const metadata` (title/description), индексируется (без noindex). Разделы:
  1) что такое cookie; 2) сейчас — только необходимые/технические (сессия/безопасность,
  сохранение выбора cookie); 3) Umami — без cookie и без идентификации; 4) аналитические/
  маркетинговые cookie (Метрика, GA, пиксели) — НЕ активны, в будущем только с предварительного
  согласия; 5) как управлять через браузер; 6) отзыв согласия; 7) контакты оператора
  (`EMAIL_CONTACT` из constants, `mailto:`) + ссылка на `/privacy`.
- **Footer** — рядом с «Политикой конфиденциальности» добавлена ссылка «Политика cookie» (`/cookie-policy`).
- **`/privacy`** — добавлена короткая встречная ссылка на `/cookie-policy` (новый раздел 6 «Файлы cookie»).
- **`sitemap.ts`** — добавлен `/cookie-policy` (priority 0.3, changeFrequency "yearly" — как `/privacy`).
- НЕ добавлял кнопку «Настройки cookie» / вызов `CookieConsent` (баннера пока нет — TASK-080).
  Страница статическая, не клиентская. Метрика/GA НЕ описаны как работающие. EMAIL/ссылки — из constants.

## Файлы

- `src/app/cookie-policy/page.tsx` (новый), `src/components/layout/Footer.tsx`,
  `src/app/privacy/page.tsx`, `src/app/sitemap.ts`.
- `.claude/reports/REPORT_TASK-081_2026-06-11.md`, CHANGELOG, SPRINT (TASK-081 → [x]).

## Критерии приёмки (прод https://gpeters.ru)

- Линт: PASS; tsc: PASS; build: PASS (`/cookie-policy` — статический ○ роут). Дельта размера: 0.
1. Роут `/cookie-policy` по шаблону privacy + metadata, индексируется. **PASS** — 200,
   `<title>Политика в отношении файлов cookie | AntiAge</title>`, noindex = 0.
2. Разделы по факту: Umami (cookieless) упомянут, «Метрика/GA … не активны», email
   `gpeters@mail.ru`, ссылка на `/privacy`, управление через браузер, отзыв согласия. **PASS**.
3. Ссылка `/cookie-policy` в Footer (на главной `href="/cookie-policy"` присутствует). **PASS**.
4. В `/privacy` встречная ссылка на `/cookie-policy` присутствует. **PASS**.
5. `/cookie-policy` в `sitemap.xml` (priority 0.3, yearly). **PASS**.

## Проблемы / открытые вопросы

- Текст — информирование по фактическому состоянию; финальная юр-проверка текстов — на гейте −1.4.
- Кнопка «Настройки cookie» и реальная блокировка скриптов появятся в TASK-080 (баннер). После
  него раздел про управление cookie можно дополнить ссылкой на настройки баннера.

## Следующий шаг

- TASK-080: cookie-баннер (vanilla-cookieconsent) с категориями и кнопкой «Настройки cookie»
  в футере; затем при подключении Метрики/GA — согласие до загрузки скриптов (юр-гейт −1.4).

## Ссылки

- CHANGELOG entry: [TASK-081]
- SPRINT.md: [TASK-081]
- Обоснование: COOKIE_CONSENT_RESEARCH.md §1, §3.
