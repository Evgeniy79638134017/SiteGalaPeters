# REPORT TASK-056b: Фикс кружков-портретов на программах — 2026-06-08

**Агент:** VS Code Claude
**Статус:** Done

## Что сделано

Исправлены три проблемы из ревью владельца (после TASK-056):

- **Лицо резалось / портрет мелкий** → перешли на готовые КВАДРАТНЫЕ фото 600×600
  (лицо по центру): `expert-circle-hero.jpg` (hero) и `expert-circle-quote.jpg` (цитата).
  `<Image>` теперь с `width=height=размер` + `object-cover` (квадрат → центрированный круг,
  обрезки нет; `object-position` не нужен).
- **«Ничего не вставилось» в hero на части программ** → hero-кружок рендерится БЕЗУСЛОВНО:
  убрана обёртка `AnimateOnScroll` (её whileInView-анимация могла оставлять портрет в
  `opacity:0`). Теперь это простой `<div>` + `<Image>`.
- **«Приблизить» у цитаты** → кружок цитаты увеличен с 96px до **112px** (`w-28 h-28`);
  hero-кружок — **140px** (`w-35 h-35`).
- Старые ссылки `expert-accent.jpg` / `expert-preview.jpg` в кружках программ заменены на
  `expert-circle-*.jpg`. `/about` (expert-about.jpg) не трогался. Новые jpg закоммичены.

## Файлы

- `src/components/programs/HealthProgramPage.tsx` — два кружка (src + width/height + object-cover).
- `public/images/expert-circle-hero.jpg`, `public/images/expert-circle-quote.jpg` (добавлены в git).
- `.claude/reports/REPORT_TASK-056b_2026-08.md` (+assets), CHANGELOG, SPRINT (TASK-056b → [x]).

## Гейты / проверки из «Цели» (прод https://gpeters.ru)

- Линт: PASS; tsc: PASS; build: PASS. Дельта размера: +2 jpg (~92 КБ), без новых зависимостей.
- (1) hero → `expert-circle-hero.jpg`, цитата → `expert-circle-quote.jpg`; `<Image>`
  width=height + `rounded-full object-cover`. **PASS**.
- (2) hero-кружок виден на ВСЕХ проверенных программах — curl по 3 разным:
  `kletochnoe-omolozhenie`, `nervnaya-sistema`, `serdechno-sosudistaya-sistema` →
  оба кружка присутствуют (hero=2/quote=2 в HTML+RSC). raw `/images/expert-circle-*.jpg` → 200. **PASS**.
- (3) рендер безусловный (убран AnimateOnScroll вокруг hero-портрета). **PASS**.
- (4) размеры: hero 140px, цитата 112px (крупнее). **PASS**.
- (5) lint/tsc/build зелёные. **PASS**.
- Скрины флагмана (Playwright, 1440px): лицо в обоих кружках по центру, не режется
  (`assets/REPORT_TASK-056b_hero.jpeg`, `_quote.jpeg`). Старый `expert-accent` на странице = 0.

## Проблемы / открытые вопросы

- Нет. (Vercel bot-checkpoint один раз блокнул headless-браузер при скрине — прошёл со 2-й
  попытки; curl-проверка не затронута.)

## Следующий шаг

- TASK-060: круглый портрет Галины в шапке слева от «AntiAge».

## Ссылки

- CHANGELOG entry: [TASK-056b]
- SPRINT.md: [TASK-056b]
- Скрины: assets/REPORT_TASK-056b_hero.jpeg, assets/REPORT_TASK-056b_quote.jpeg
