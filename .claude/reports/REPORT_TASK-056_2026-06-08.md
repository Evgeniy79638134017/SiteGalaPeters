# REPORT TASK-056: Круглые фото Галины на страницах программ — 2026-06-08

**Агент:** VS Code Claude
**Статус:** Done

## Что сделано

В шаблон страницы программы (`HealthProgramPage.tsx`) добавлены два круглых портрета
Галины через next/image — как на доксвете:

- **Hero** — круглый портрет справа-сверху (на мобильном — сверху по центру):
  `expert-accent.jpg`, 128px (mobile) → 144px (desktop), `rounded-full` + `object-cover`
  + белое кольцо/тень. Hero перестроен в `md:flex` (текст слева, портрет справа через `md:order-2`).
- **Блок цитаты «Главный закон»** — круглый портрет слева от цитаты: `expert-preview.jpg`,
  80px → 96px, `rounded-full` + `object-cover object-top`. Блок стал `flex` (на мобильном — стек).
- Везде `alt="Галина — эксперт по anti-age"`, `sizes` заданы. Композиция/тексты не менялись,
  новые фото-файлы не плодились (взяты из `public/images`). 3 страницы «китов» не тронуты.

## Файлы

- `src/components/programs/HealthProgramPage.tsx` — импорт next/image + 2 портрета.
- `.claude/reports/REPORT_TASK-056_2026-06-08.md`, CHANGELOG, SPRINT (TASK-056 → [x]).
- Скрины: `.claude/reports/assets/REPORT_TASK-056_{hero,quote}.jpeg`.

## Гейты / проверки из «Цели» (прод https://gpeters.ru)

- Линт: PASS; tsc: PASS; build: PASS. Дельта размера: 0 (только разметка, без зависимостей).
- (1) Hero: круглый `expert-accent.jpg` — на проде next/image-srcset присутствует. **PASS**.
- (2) У цитаты: круглый `expert-preview.jpg` — присутствует. **PASS**.
- (3) next/image, `rounded-full`+`object-cover`, фикс. размеры, alt «Галина — эксперт по anti-age». **PASS**.
- (4) lint/tsc/build зелёные. **PASS**.
- Прод проверен на 2 программах: `nervnaya-sistema` и `serdechno-sosudistaya-sistema` —
  оба портрета в HTML (next/image), alt на месте; страница «кита» `biochemistry` не изменена
  (expert-accent = 0). Скрины hero и блока цитаты приложены (Playwright, 1440px).

## Проблемы / открытые вопросы

- Нет. Портреты берутся из уже залитых фото; реф-код/составы не затрагивались.

## Следующий шаг

- TASK-057: кнопка «Скачать таблицу приёма» → JPG расписания (html2canvas).

## Ссылки

- CHANGELOG entry: [TASK-056]
- SPRINT.md: [TASK-056]
- Скрины: assets/REPORT_TASK-056_hero.jpeg, assets/REPORT_TASK-056_quote.jpeg
