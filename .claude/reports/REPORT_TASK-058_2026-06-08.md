# REPORT TASK-058: Обложки статей блога — 2026-06-08

**Агент:** VS Code Claude
**Статус:** Done

## Что сделано

- В `interface BlogPost` добавлено поле `coverImage: string`; каждой из 6 статей проставлен
  явный путь `/images/blog/<slug>.jpg`.
- Пустые заглушки `aspect-16/10 bg-brand-soft/30` заменены на `<Image fill object-cover>`
  с обложкой:
  - `BlogPreview.tsx` (превью блога на главной) — 3 карточки;
  - `app/blog/page.tsx` (список /blog) — все карточки;
  - `app/blog/[slug]/page.tsx` (шапка статьи) — обложка в hero (контейнер переведён на
    `aspect-16/10` под соотношение обложек, `priority`).
- `alt` = заголовок статьи. 6 оптимизированных jpg (≈79–114 КБ) закоммичены в
  `public/images/blog/`. Тексты статей не менялись.

## Файлы

- `src/content/blog-posts.ts` (+поле coverImage в интерфейс и 6 статей).
- `src/components/sections/BlogPreview.tsx`, `src/app/blog/page.tsx`,
  `src/app/blog/[slug]/page.tsx` — Image вместо заглушек.
- `public/images/blog/*.jpg` (6 файлов).
- `.claude/reports/REPORT_TASK-058_2026-06-08.md` (+asset), CHANGELOG, SPRINT (TASK-058 → [x]).

## Гейты / проверки из «Цели» (прод https://gpeters.ru)

- Линт: PASS; tsc: PASS; build: PASS. Дельта размера: +6 jpg (~0.58 МБ), без зависимостей.
- (1) `coverImage` в интерфейсе + у всех 6 статей. **PASS**.
- (2) Заглушки заменены на `<Image>` (fill + object-cover + sizes) в BlogPreview и /blog. **PASS**.
- (3) Шапка статьи показывает обложку. **PASS**.
- (4) `alt` = заголовок. **PASS**.
- (5) lint/tsc/build зелёные; прод:
  - `/blog` — 6 разных обложек (next/image), все `<img>` загружены (naturalWidth>0);
  - главная (BlogPreview) — 3 обложки;
  - статья `kak-ya-vernula-zrenie-v-62` — обложка в hero;
  - raw `/images/blog/<slug>.jpg` ×6 → 200. **PASS**.

Скрин: `assets/REPORT_TASK-058_blog.jpeg` (список /blog с картинками).

## Проблемы / открытые вопросы

- Нет. Обложки 16:10, контейнеры 16:10 → без обрезки.

## Следующий шаг

- SPRINT-3 по медиа-контенту закрыт (видео-hero, фото, программы, обложки, шапка, download).
  Дальше — по приоритетам Cowork (напр. TASK-014 email-ESP ждёт решения владельца;
  вставка реф-кода agenyz в ORDER_URL; юр.ревизия текстов программ — гейт −1.4).

## Ссылки

- CHANGELOG entry: [TASK-058]
- SPRINT.md: [TASK-058]
- Скрин: assets/REPORT_TASK-058_blog.jpeg
