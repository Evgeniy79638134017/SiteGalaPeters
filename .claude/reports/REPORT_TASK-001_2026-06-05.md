# REPORT TASK-001: Lint-фикс CountUp.tsx — 2026-06-05

**Агент:** VS Code Claude
**Статус:** Done

## Что сделано

- Починена единственная lint-ошибка проекта: `react-hooks/set-state-in-effect` в
  `CountUp.tsx:44` (синхронный `setCount(end)` в теле `useEffect` для ветки
  `prefers-reduced-motion`).
- Вместо синхронного `setState` конечное значение теперь выводится производным значением при
  рендере (`displayCount = shouldReduceMotion && hasStarted ? end : count`). Эффект для
  reduced-motion просто выходит рано — setState из тела эффекта убран.
- Поведение счётчика сохранено: анимация 0→end при обычном режиме; при `prefers-reduced-motion`
  сразу показывается конечное значение после появления в зоне видимости. Пропсы не менялись.

## Файлы

- `antiage-platform/src/components/shared/CountUp.tsx` — убран `setCount(end)` из эффекта,
  добавлено производное `displayCount` в рендере. Других файлов не трогал.
- `.claude/reports/REPORT_TASK-001_2026-06-05.md`, `.claude/logs/CHANGELOG.md`,
  `.claude/tasks/SPRINT.md` (TASK-001 → [x]).

## Гейты

- Сборка: N/A (gate задачи — lint; сборка не запрашивалась, изменение тривиальное и типобезопасное)
- Линт (`npx eslint .`): PASS — без ошибок и предупреждений (был 1 error).
- Проверка типов (`npx tsc --noEmit`): PASS — без ошибок.
- Дельта размера: незначительная (−1 setState-ветка, +1 производное значение; чистый бандл не меняется).

## Проблемы / открытые вопросы

- нет. Минимальное вмешательство: тронут только проблемный файл, публичные пропсы CountUp
  не изменены, библиотеки не добавлялись.

## Следующий шаг

- Cowork: принять задачу (lint-gate теперь зелёный для всего проекта — снимает оговорку из
  REPORT_TASK-019). Дальше по SPRINT-1: TASK-002 (статьи в sitemap), TASK-003 (дисклеймеры),
  TASK-004 (единый email-валидатор).

## Ссылки

- CHANGELOG entry: [TASK-001]
- SPRINT.md: [TASK-001]
