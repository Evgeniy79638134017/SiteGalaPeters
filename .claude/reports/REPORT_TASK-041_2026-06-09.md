# REPORT TASK-041: Гигиена git — игнор ContentFiles/, коммит .claude/ — 2026-06-09

**Агент:** VS Code Claude
**Статус:** Done

## Что сделано

- **.gitignore** (корень `siteGala/`): добавлено правило **`/ContentFiles/`** (именно каталог
  исходников). НЕ глобальные `*.mp4`/`*.CR3` — чтобы не задеть ассеты сайта в
  `antiage-platform/public/` (там лежит `public/media/hero-bg.mp4`). Существующие правила не трогал.
- **Код не импортирует из ContentFiles**: grep по `antiage-platform/src`, `antiage-api/src`,
  `next.config.*` — ссылок нет (НЕ BLOCKER).
- **`git rm -r --cached ContentFiles`**: из индекса удалены 13 отслеживаемых файлов
  (11 транскриптов `ANTIAGE_*.txt` + `photo_2026-03-26_13-43-57.jpg`, `…44-01.jpg`).
  Файлы остались на диске (перестали отслеживаться). Тяжёлые исходники (`dudnikpromo2.mp4` 85 МБ,
  `_MG_4815 (1).CR3` 23 МБ, прочие фото) и так НЕ были в git.
- **Закоммичен `.claude/`** (`git add .claude/`): добавлен координационный слой — 41 файл:
  `prompts/PROMPT_*` (включая TEMPLATE), `tasks/BACKLOG.md`, `context/ARCHITECTURE.md`,
  `README_СИСТЕМА.md`, `reports/_TEMPLATE.md`, `settings.json`. (Отчёты/CHANGELOG/SPRINT уже были
  в git — 46 файлов.)
- **settings.json безопасен**: только `permissions.allow` (список команд) + `additionalDirectories:
  ["antiage-platform"]` (относительный путь). Секретов и абсолютных путей нет.

## Проверки из «Цели»

- (1) `/ContentFiles/` в `.gitignore`. **PASS**.
- (2) Код не ссылается на ContentFiles. **PASS**.
- (3) `git rm -r --cached ContentFiles` выполнен; файлы на диске сохранены. **PASS**.
- (4) `.claude/` закоммичен без секретов (settings.json = permissions). **PASS**.
- (5) Контрольные выводы:
  - `git ls-files | grep -iE 'ContentFiles|dudnikpromo|\.CR3'` → **пусто**.
  - `git ls-files .claude/...` → `settings.json`, `tasks/BACKLOG.md`, `context/ARCHITECTURE.md`,
    `prompts/PROMPT_TEMPLATE.md`, `reports/_TEMPLATE.md` присутствуют.
  - `git ls-files antiage-platform/public | wc -l` = **20** (включая `public/media/hero-bg.mp4`,
    `public/images/hero-poster.jpg`) — public НЕ затронут.
  **PASS**.

## Гейты / безопасность

- Историю git НЕ переписывал; файлы с диска НЕ удалял; `public/` не игнорировал.
- Сборка/линт/типы: N/A (изменения только в git-индексе/.gitignore/.claude, код не трогался).
- Дельта размера (бандл): 0. Из git-индекса убраны 13 текст/фото-исходников; .claude — лёгкие .md/.json.

## Проблемы / открытые вопросы

- Нет. Тяжёлые бинарники ContentFiles в историю никогда не попадали — переписывание истории не нужно.

## Следующий шаг

- Разово после `git add .` проверять `git status`, что ContentFiles не всплывают (правило закрывает).
- Опц. TASK-040: синхронизация устаревших данных в CLAUDE.md ч.II (Next 15→16 и пр.).

## Ссылки

- CHANGELOG entry: [TASK-041]
- SPRINT.md: [TASK-041]
