# REPORT TASK-052: Реальные фото на сайт (next/image) — 2026-06-06

**Агент:** VS Code Claude
**Статус:** Done

## Что сделано

- Плейсхолдеры «Фото эксперта» заменены реальными фото через next/image:
  - AboutPreview (главная): `expert-preview.jpg` (…36, в рост) — `<Image fill>` в существующем
    контейнере `aspect-square` (object-cover object-top), паттерн как у Hero-постера (TASK-051).
  - /about (hero): добавлена колонка-портрет `expert-about.jpg` (…32, деловой портрет) —
    `<Image>` с интринсик-размерами 901×1280, `priority`, адаптивная ширина (max-w-sm).
- `expert.jpg` (копия …32) добавлен для `personJsonLd.image` (закрывает аудит M4) — jsonld.ts
  уже ссылался на `/images/expert.jpg`, теперь файл существует.
- `expert-accent.jpg` (…39) скопирован по тех-параметрам, но НЕ размещён: подходящего «малого»
  плейсхолдера нет (аватары в Testimonials — другие люди, не эксперт), по заданию — «если нет, не использовать».
- …27 и тканевые фото не использованы. Тексты секций не менялись, видео-hero не трогался.

## Файлы

- `antiage-platform/src/components/sections/AboutPreview.tsx` — placeholder → `<Image fill>`.
- `antiage-platform/src/app/about/page.tsx` — портрет в hero (`<Image>` 901×1280, priority).
- `antiage-platform/public/images/`: `expert-about.jpg`, `expert-preview.jpg`, `expert.jpg`,
  `expert-accent.jpg` (резерв, не размещён).
- `.claude/reports/REPORT_TASK-052_2026-06-06.md`, CHANGELOG, SPRINT (TASK-052 → [x]).

## Гейты

- Линт: PASS; tsc: PASS; build: PASS.
- Дельта размера: +4 изображения (~375 КБ) в git; без новых npm-зависимостей.

## Проверки из «Цели» (на проде https://gpeters.ru)

1. AboutPreview (главная) и /about показывают реальные фото:
   - `/` → `/_next/image?url=%2Fimages%2Fexpert-preview.jpg&w=384&q=75` (200);
   - `/about` → `/_next/image?url=%2Fimages%2Fexpert-about.jpg&w=640&q=75` (200);
   - raw `/images/expert-about.jpg`, `expert-preview.jpg` → 200 image/jpeg. **PASS**.
   - *Скриншоты desktop 1440 НЕ приложены:* Playwright MCP-сервер в этой сессии недоступен
     (отключён). Верификация выполнена curl'ом по проду (URL next/image + коды 200 + alt).
     Скрины сделаю при восстановлении Playwright, если потребуется для приёмки.
2. `grep "Фото эксперта"` по `src/` → не найдено; на проде `/about` → 0 вхождений. **PASS**.
3. next/image с alt/sizes: alt="Галина — эксперт по anti-age" присутствует; about — intrinsic
   width/height 901×1280 + sizes; AboutPreview — fill + sizes (контейнер aspect-square). **PASS**.
4. `personJsonLd.image` = `/images/expert.jpg` → файл существует (200 image/jpeg). **PASS**.
5. lint/tsc/build зелёные. **PASS**.

## Проблемы / открытые вопросы

- Screenshots не приложены — Playwright MCP отключён в сессии (инфраструктурное ограничение,
  не код). Вместо них — curl-доказательства (next/image-URL, HTTP 200, alt) выше.
- `expert-accent.jpg` лежит в public, но в UI не используется (нет подходящего малого слота;
  по заданию). Можно задействовать позже по решению Cowork/владельца.
- …27 ожидает уточнения владельца (по SPRINT) — не трогал.

## Следующий шаг

- TASK-053: каталог программ по структуре doksveta.ru/programs (драфт контента — Cowork).

## Ссылки

- CHANGELOG entry: [TASK-052]
- SPRINT.md: [TASK-052]
