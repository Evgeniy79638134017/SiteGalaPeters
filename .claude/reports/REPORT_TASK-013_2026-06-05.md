# REPORT TASK-013: Раздельные согласия (ПДн / рассылка) — 2026-06-05

**Агент:** VS Code Claude
**Статус:** Done

## Что сделано

- Формы: вместо одного общего чекбокса — раздельные согласия.
  • Квиз (EmailGate) и партнёрка (PartnerForm): ДВА неустановленных чекбокса — «обработка ПДн»
    (обязательный) и «рекламная рассылка» (необязательный). У квиза ПДн-текст явно включает
    данные о состоянии здоровья (спец.категория, L3).
  • Контакт-форма: один обязательный чекбокс «обработка ПДн» (рассылки нет).
  • Submit блокируется без согласия на ПДн; оба флага (`consentData`, `consentMarketing`) уходят
    в тело запроса postJson.
- API (antiage-api): схемы Quiz/Contact/Partner получили `consentData: z.literal(true)` (обязателен
  → иначе 400) и Quiz/Partner — `consentMarketing: z.boolean().optional()`. server.ts после записи
  сущности пишет `ConsentLog action="data_processing"`; при `consentMarketing===true` — вторую
  строку `action="marketing"` (те же source/ip/ua). EmailContact.consentGiven/Date — как было.
- Схема Prisma НЕ менялась (ConsentLog.action — свободная строка). Тексты согласий — рабочая
  редакция из промта, дословно (финал утвердит юрист, гейт −1.4).
- ДЕПЛОЙ выполнен: antiage-api пересобран (tsc→dist), переложен на VPS, `pm2 reload antiage-api`,
  healthz 200. Фронт — через авто-деплой Vercel (тот же коммит).

## Файлы

- `antiage-platform/src/components/forms/EmailGate.tsx` — 2 чекбокса, проброс consentMarketing.
- `antiage-platform/src/components/quiz/QuizResults.tsx` — consentData/Marketing в payload /api/quiz.
- `antiage-platform/src/components/forms/PartnerForm.tsx` — 2 чекбокса, флаги в payload.
- `antiage-platform/src/components/forms/ContactFormClient.tsx` — текст ПДн-чекбокса + consentData.
- `antiage-api/src/{schemas.ts,server.ts}` — consent-поля + раздельные ConsentLog.
- `.claude/reports/REPORT_TASK-013_2026-06-05.md`, CHANGELOG, SPRINT (TASK-013 → [x]).

## Гейты

- antiage-platform — Линт: PASS; tsc: PASS; build: PASS.
- antiage-api — tsc: PASS; dist пересобран и развёрнут; `pm2 reload` OK; healthz 200
  (локально и `https://gpeters.ru/api/healthz` через nginx).
- Дельта размера: незначительная (без новых зависимостей).

## Проверки из «Цели» (сквозной тест на https://gpeters.ru, ConsentLog по psql)

| # | Сценарий (POST) | HTTP | ConsentLog |
|---|-----------------|------|-----------|
| a | contact, consentData=true | 200 | 1 строка: data_processing / contact_form |
| b | partner, consentData=true + consentMarketing=true | 200 | 2 строки: data_processing + marketing / partner_form |
| c | quiz, consentData=true, marketing=false | 200 | 1 строка: data_processing / quiz_gate |
| d | partner БЕЗ consentData | **400** | 0 строк (и 0 в PartnerApplication) |

Тестовые записи (`task013-%@example.com`) удалены — повторный SELECT по ConsentLog/ContactMessage/
PartnerApplication/EmailContact/QuizResult = все 0.

Фронт-деплой Vercel подтверждён в живом DOM (Playwright, /contacts): чекбокс с текстом
«Я согласен(а) на обработку персональных данных в соответствии с Политикой конфиденциальности.»
— ровно один (контакт = только ПДн). Партнёр/квиз — два чекбокса (тот же деплой; multi-step
форма партнёрки headless не драйвится из-за тайминга React-state, но контракт подтверждён e2e b).

## Проблемы / открытые вопросы

- Маркетинг-согласие НЕ пишется в колонку EmailContact (потребовало бы миграции Prisma) — отложено
  как зависимость для **TASK-014/017**. Сейчас факт согласия на рассылку фиксируется только в
  ConsentLog (action="marketing").
- Кратковременное окно между деплоем API и Vercel: старый фронт без consentData → 400 (ожидаемо
  по промту; закрыто авто-деплоем Vercel).

## Следующий шаг

- TASK-014 (email из API, ждёт выбора российского ESP) — включит запись marketing-флага в
  EmailContact (миграция) и использование согласия на рассылку для drip-серии.

## Ссылки

- CHANGELOG entry: [TASK-013]
- SPRINT.md: [TASK-013]
