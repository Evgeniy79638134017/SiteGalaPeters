# REPORT TASK-019: Переключение форм фронта на /api + домен gpeters.ru — 2026-06-05

**Агент:** VS Code Claude
**Статус:** Done

## Что сделано

- Формы сайта переведены с server actions-заглушек Vercel на same-origin POST в наш API
  (`/api/contact`, `/api/partner`, `/api/quiz`) через хелпер `src/lib/api.ts`. ПДн теперь
  пишутся сразу в российскую БД (через Nginx-прокси /api → VPS, CORS не нужен).
- Квиз: реализован реальный сабмит (был TODO-stub) — `submitQuiz` → `postJson("/api/quiz")`;
  `resultToken` из ответа API сохраняется и доходит до экрана результатов.
- Удалён каталог `src/actions/` целиком (3 файла); нигде не импортируется (tsc/build подтвердили).
- Домен: дефолт `https://antiage.ru` → `https://gpeters.ru` во всех местах (layout
  metadataBase/canonical/og:url, sitemap, robots, jsonld, share-URL блога). `NEXT_PUBLIC_SITE_URL`
  из env остаётся приоритетом. Контактный email `hello@antiage.ru` → `gpeters@mail.ru`.
- `antiage-platform/vercel.json`: `ignoreCommand` пропускает сборку, если коммит не трогал
  файлы внутри antiage-platform (наши .claude-коммиты не тратят билды).
- UX/тексты/состояния форм и чекбоксы согласий НЕ менялись (это TASK-013). antiage-api не трогался.

## Файлы

- `src/lib/api.ts` — новый fetch-хелпер `postJson<T>` (контракт {success, error?} зеркален actions).
- `src/components/forms/ContactFormClient.tsx`, `PartnerForm.tsx` — submit через postJson.
- `src/components/quiz/QuizResults.tsx` — реальный сабмит квиза + проброс resultToken.
- `src/app/layout.tsx`, `sitemap.ts`, `robots.ts`, `src/lib/jsonld.ts`, `src/app/blog/[slug]/page.tsx`
  — дефолтный SITE_URL → gpeters.ru.
- `src/lib/constants.ts` — EMAIL_CONTACT → gpeters@mail.ru.
- `antiage-platform/vercel.json` — ignoreCommand.
- Удалены: `src/actions/{submitContact,submitPartner,submitQuiz}.ts`.
- `.claude/reports/REPORT_TASK-019_2026-06-05.md`, CHANGELOG, SPRINT (TASK-019 → [x]).

## Гейты

- Сборка (`npm run build`): PASS — 23 страницы собрались, без ошибок.
- Проверка типов (`npx tsc --noEmit`): PASS — без ошибок (удаление actions не оставило висячих импортов).
- Линт (`npm run lint`): 1 ошибка — `CountUp.tsx` (`react-hooks/set-state-in-effect`). Это
  ПРЕД-СУЩЕСТВУЮЩИЙ долг и отдельная задача TASK-001, файл этой задачей не трогался; все
  файлы, затронутые TASK-019, линт проходят. На сборку Vercel не влияет (build зелёный).
- Дельта размера: зависимости не добавлялись (нативный fetch). Кода: −141/+68 строк (минус actions).
- Проверки из «Цели» (выводы команд):
  1. На https://gpeters.ru POST на /api/contact, /api/partner, /api/quiz → HTTP 200; в БД
     EmailContact=1, QuizResult=1, ContactMessage=1, PartnerApplication=1, ConsentLog=3
     (contact_form/partner_form/quiz_gate, ipAddress=внешний 156.146.34.241). Тестовые записи
     (`task019-test@example.com`) удалены (повторный SELECT → все 0). **PASS**.
  2. HTML прода: `<link rel="canonical" href="https://gpeters.ru"/>` и
     `<meta property="og:url" content="https://gpeters.ru"/>`. **PASS**.
  3. `src/actions/` удалён, импортов нет (tsc/build зелёные). **PASS**.
  4. tsc PASS, build PASS; lint — только пред-существующая ошибка CountUp (TASK-001). **PASS (с оговоркой)**.
  5. Пуш в main → Vercel auto-deploy; прод отдаёт новый canonical (=деплой Ready). **PASS**.
- Квиз resultToken: `POST /api/quiz` → `{"success":true,"resultToken":"…"}`; клиент сохраняет
  его в state и пробрасывает на экран результатов (data-result-token). **OK**.

## Проблемы / открытые вопросы

- `vercel.json ignoreCommand` = `git diff --quiet HEAD^ HEAD ./` (точный пример из доков Vercel:
  exit 0 = пропустить сборку, exit 1 = собирать; команда исполняется из Root Directory =
  antiage-platform). Коммит-код собрался; коммит этого отчёта (только .claude/) сборку пропустит.
- Линт-долг CountUp.tsx остаётся за TASK-001 — намеренно не трогал (разделение задач).
- Соцсети (@antiage Telegram/YouTube/Instagram) НЕ менял — уточнит владелец (по промту).

## Следующий шаг

- **TASK-014:** отправка email из API (после записи в БД) — ждёт выбора российского ESP
  (открытый ВОПРОС в SPRINT).
- **TASK-013:** раздельные согласия (ПДн / рассылка) на формах и в ConsentLog.

## Ссылки

- CHANGELOG entry: [TASK-019]
- SPRINT.md: [TASK-019]
